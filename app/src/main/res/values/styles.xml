<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Light Theme -->
    <style name="LightTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/lightBackground</item>
        <item name="android:textColor">@color/lightText</item>
        <item name="android:fontFamily">@font/whiteamour</item>
    </style>

    <!-- Dark Theme (Blue) -->
    <style name="DarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/bluePrimary</item>
        <item name="colorPrimaryDark">@color/bluePrimaryDark</item>
        <item name="colorAccent">@color/blueAccent</item>
        <item name="android:windowBackground">@color/blueBackground</item>
        <item name="android:textColor">@color/blueText</item>
        <item name="android:fontFamily">@font/sanseriffic</item>
    </style>

    <!-- Custom Theme -->
    <style name="CustomTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/customBackground</item>
        <item name="colorPrimaryDark">@color/customBackground</item>
        <item name="colorAccent">@color/customText</item>
        <item name="android:windowBackground">@color/customBackground</item>
        <item name="android:textColor">@color/customText</item>
        <item name="android:fontFamily">@font/soft_sans_serif_7</item>
    </style>
</resources>
